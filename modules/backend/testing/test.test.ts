import { expect } from 'chai';
import { describe, it } from 'mocha';

describe('Test', () => {
  it('Test per configurare mocha', () => {
    // eslint-disable-next-line no-unused-expressions
    expect(true).to.be.true;
  });
});
